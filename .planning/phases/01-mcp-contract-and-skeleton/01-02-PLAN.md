---
phase: 01-mcp-contract-and-skeleton
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/contracts/review.ts
  - src/errors.ts
  - src/tools/review.ts
  - tests/contract/review-tool.test.ts
  - README.md
  - docs/mcp-contract.md
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - MCP-03
must_haves:
  truths:
    - "Valid review requests produce deterministic JSON that passes the exported response schema."
    - "Rejected requests return machine-readable error objects with stable codes."
    - "The repository documents server startup, request inputs, supported evidence types, limits, and output semantics."
    - "Contract-level tests prove discovery/invocation behavior and malformed request handling."
  artifacts:
    - path: "src/contracts/review.ts"
      provides: "Runtime Zod schemas plus inferred TypeScript contracts"
      exports: ["reviewRequestSchema", "reviewResponseSchema", "reviewToolResultSchema"]
    - path: "src/errors.ts"
      provides: "Stable machine-readable error helper"
      exports: ["EvidenceLensError", "toToolErrorResult"]
    - path: "tests/contract/review-tool.test.ts"
      provides: "Contract tests for tool schema, deterministic success, and validation failures"
      contains: "review_evidence"
    - path: "docs/mcp-contract.md"
      provides: "MCP client contract documentation"
      contains: "review_evidence"
  key_links:
    - from: "src/tools/review.ts"
      to: "src/contracts/review.ts"
      via: "schema parse before response creation"
      pattern: "reviewRequestSchema\\.parse"
    - from: "tests/contract/review-tool.test.ts"
      to: "src/server.ts"
      via: "createServer-based contract invocation or SDK client transport"
      pattern: "createServer"
    - from: "README.md"
      to: "docs/mcp-contract.md"
      via: "contract documentation link"
      pattern: "docs/mcp-contract.md"
---

<objective>
Lock the Phase 1 MCP contract with runtime schemas, machine-readable errors, tests, and documentation.

Purpose: MCP clients need predictable success and rejection semantics before later phases add evidence access and providers.
Output: Runtime validation, stable error objects, contract-level tests, and developer/client documentation.
</objective>

<execution_context>
@/Users/yifeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yifeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@DEVELOPMENT.md
@.planning/phases/01-mcp-contract-and-skeleton/01-mcp-contract-and-skeleton-01-SUMMARY.md
@src/server.ts
@src/contracts/review.ts
@src/tools/review.ts

Plan 02 depends on Plan 01. Keep Phase 1 provider-free and evidence-reader-free. Do not implement DeepSeek, Docker, allowlisted file reads, parsing for text/PDF/image/table evidence, or review orchestration.

Official MCP TypeScript SDK planning reference checked 2026-08-22:
- https://ts.sdk.modelcontextprotocol.io/v2/
- https://github.com/modelcontextprotocol/typescript-sdk

Use the current SDK registration style from Plan 01 and replace the permissive skeleton schema with strict `zod/v4` schemas.
</context>

<must_haves>
Truths:
- MCP-01: A contract test can discover or directly inspect that the server exposes `review_evidence`.
- MCP-02: Valid requests produce schema-valid JSON; invalid requests produce stable machine-readable errors.
- MCP-03: README and contract docs describe inputs, supported evidence types, limits, and response/error semantics.

Artifacts:
- `src/contracts/review.ts` exports Zod schemas and inferred TypeScript types.
- `src/errors.ts` exports stable error-code helpers.
- `tests/contract/review-tool.test.ts` proves valid and invalid contract behavior.
- `README.md` and `docs/mcp-contract.md` document local development and MCP usage.

Key Links:
- Tool handler parses input with `reviewRequestSchema` before creating a response.
- Tests validate both `reviewResponseSchema` and machine-readable error shape.
- README links to `docs/mcp-contract.md` and shows `npm run dev`.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runtime schemas and stable error results</name>
  <files>src/contracts/review.ts, src/errors.ts, tests/contract/review-tool.test.ts</files>
  <read_first>
    - @src/contracts/review.ts
    - @.planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    - `reviewRequestSchema.safeParse(validRequest).success` is true for a request with `reviewId`, `objective`, metadata-only evidence, and limits within bounds.
    - Requests with more than 20 evidence items, an objective longer than 4000 characters, unsupported evidence type, empty evidence id, or path-like control characters in `reference` fail validation.
    - `reviewToolResultSchema.parse(toolResult)` succeeds for the MCP text-content wrapper returned by success and error helpers.
    - `toToolErrorResult(error)` returns JSON text content with `ok: false`, stable `code`, and sanitized `message`.
    - Schema/error tests in `tests/contract/review-tool.test.ts` run before tool handler wiring exists and only import `src/contracts/review.ts` and `src/errors.ts`.
  </behavior>
  <action>Replace interface-only contracts with Zod schemas plus inferred exported types. Define `evidenceRoleSchema` values `assignment_brief`, `rubric`, `teacher_instructions`, `solution`, and `other`; define `evidenceTypeSchema` values `text`, `pdf`, `image`, `screenshot`, and `table`; define `reviewRequestSchema` with `reviewId` 1-128 characters, `objective` 1-4000 characters, `evidence` defaulting to an empty array with max 20 items, and `limits` with optional `maxEvidenceItems` 1-20 and `maxObjectiveLength` 1-4000. Evidence `reference` remains an opaque string but must reject null bytes and ASCII control characters to reduce future path/input injection risk without implementing filesystem policy early. Define `reviewResponseSchema` so successful responses include deterministic metadata with `serverName`, `serverVersion`, and `generatedAt`, where `generatedAt` is supplied by a fixed/injected Phase 1 clock rather than wall-clock time. Define and export `reviewToolResultSchema` for the MCP text-content wrapper shape returned by the tool, and require tests to parse success and error wrappers with it. Create `src/errors.ts` with `EvidenceLensErrorCode` values `INVALID_REQUEST`, `UNSUPPORTED_EVIDENCE_TYPE`, `LIMIT_EXCEEDED`, and `INTERNAL_ERROR`, an `EvidenceLensError` class, and `toToolErrorResult()` that returns MCP text content containing sanitized JSON. Add the initial `tests/contract/review-tool.test.ts` sections for schema acceptance/rejection, MCP text-content wrapper validation, and error serialization only; leave handler/server invocation tests for Task 2.</action>
  <verify>
    <automated>npm test -- tests/contract/review-tool.test.ts --testNamePattern="schema|error" && npm run build</automated>
  </verify>
  <acceptance_criteria>Schemas enforce Phase 1 limits, error JSON is stable and sanitized, and no schema or error helper reads files, secrets, or provider configuration.</acceptance_criteria>
  <done>Schemas enforce Phase 1 limits, error JSON is stable and sanitized, and no schema or error helper reads files, secrets, or provider configuration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire validation into the review tool and add contract tests</name>
  <files>src/tools/review.ts, tests/contract/review-tool.test.ts</files>
  <read_first>
    - @src/server.ts
    - @src/tools/review.ts
    - @src/contracts/review.ts
    - @src/errors.ts
  </read_first>
  <behavior>
    - A valid minimal request returns a JSON text content item where `ok: true`, `reviewToolResultSchema.parse(toolResult)` succeeds for the MCP wrapper, and `reviewResponseSchema.parse(parsedJson)` succeeds for the JSON payload.
    - The input identity remains `ReviewRequest.reviewId`; successful responses set `ReviewResponse.requestId = request.reviewId`.
    - Repeating the same valid request returns exactly equal JSON content, including metadata; if `generatedAt` is present, it comes from a fixed/injected clock and is asserted as an exact value.
    - Invalid objective/evidence payloads return `ok: false` with code `INVALID_REQUEST` or `LIMIT_EXCEEDED`.
    - Server construction exposes exactly the expected Phase 1 review tool and no write/mutation tools.
  </behavior>
  <action>Update `src/tools/review.ts` so `review_evidence` uses `reviewRequestSchema` as the MCP `inputSchema`, validates handler input before response creation, and returns `toToolErrorResult()` for validation failures. Keep the successful response deterministic per MCP-02: keep the input identity field as `ReviewRequest.reviewId` and explicitly set `ReviewResponse.requestId = request.reviewId`, set `status: "accepted"`, empty `findings`, and metadata with server name/version plus `generatedAt` from a fixed constant or injectable default clock. Repeated identical valid requests must stringify to exactly equal JSON. Extend `tests/contract/review-tool.test.ts` with handler/server tests that assert the `reviewId` to `requestId` mapping, exact repeated-response equality, exact deterministic `generatedAt`, MCP text-content wrapper validation with `reviewToolResultSchema`, schema-valid success, malformed request handling, limit enforcement, and no accidental mutation/write tool exposure. Prefer invoking the registered tool through official SDK client/server helpers if straightforward; otherwise export a pure `handleReviewRequest(input, options?)` function from `src/tools/review.ts` and test that function plus a server registration inspection from `createServer()`. Do not add network, provider, filesystem, or snapshot tests.</action>
  <verify>
    <automated>npm test -- tests/contract/review-tool.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>Contract tests pass for valid invocation, explicit `reviewId` to `requestId` mapping, MCP text-content wrapper validation, malformed request handling, limit enforcement, and no accidental mutation/write tool exposure.</acceptance_criteria>
  <done>Contract tests pass for valid invocation, explicit `reviewId` to `requestId` mapping, MCP text-content wrapper validation, malformed request handling, limit enforcement, and no accidental mutation/write tool exposure.</done>
</task>

<task type="auto">
  <name>Task 3: Document the local MCP contract</name>
  <files>README.md, docs/mcp-contract.md</files>
  <read_first>
    - @DEVELOPMENT.md
    - @.planning/REQUIREMENTS.md
    - @src/contracts/review.ts
  </read_first>
  <action>Create `README.md` with project purpose, local setup commands (`npm install`, `npm run dev`, `npm test`, `npm run build`), and a link to `docs/mcp-contract.md`. Create `docs/mcp-contract.md` documenting the `review_evidence` tool name, transport (`stdio`), request JSON fields including `reviewId` as the request identity, supported evidence roles/types, Phase 1 limits (`maxObjectiveLength: 4000`, `maxEvidenceItems: 20`), success response fields including the explicit mapping `response.requestId = request.reviewId`, error response shape and codes, and explicit Phase 1 non-capabilities: no local file reads, no writes/deletes/mutations, no provider calls, no Docker deployment yet. Include one valid request example and one malformed request/error example. Keep examples provider-neutral and do not mention DeepSeek implementation details beyond later-phase non-capability.</action>
  <verify>
    <automated>npm test && npm run build && test -f README.md && test -f docs/mcp-contract.md && grep -v '^#' docs/mcp-contract.md | grep -q 'review_evidence' && grep -v '^#' docs/mcp-contract.md | grep -q 'maxEvidenceItems'</automated>
  </verify>
  <acceptance_criteria>Docs explain how an MCP client discovers/invokes the server, list supported inputs and limits, define the `reviewId` to `requestId` output mapping and error semantics, and avoid promising later-phase evidence/provider features.</acceptance_criteria>
  <done>Docs explain how an MCP client discovers/invokes the server, list supported inputs and limits, define the `reviewId` to `requestId` output mapping and error semantics, and avoid promising later-phase evidence/provider features.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> Zod schema | Untrusted JSON must be validated before handler logic. |
| Request reference strings -> future filesystem layer | Opaque references may later become path inputs and must not normalize unsafe characters silently. |
| Error helper -> MCP client | Validation failures cross back to the client and must not leak internals or secrets. |
| Contract tests -> implementation | Tests must catch accidental expansion of capabilities beyond read-only review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | `reviewRequestSchema` evidence references | mitigate | Reject null bytes and control characters; document references as opaque metadata until Phase 3 allowlist enforcement. |
| T-01-07 | Denial of Service | Objective/evidence payloads | mitigate | Enforce 4000-character objective limit and 20-item evidence limit; test limit failures. |
| T-01-08 | Elevation of Privilege | Tool registry | mitigate | Contract test asserts only read-only `review_evidence` capability is exposed; no write/delete/mutate tools. |
| T-01-09 | Information Disclosure | Error serialization | mitigate | `toToolErrorResult()` sanitizes messages and returns stable codes without stack traces, env vars, raw filesystem paths, or provider details. |
| T-01-10 | Malformed Request Handling | Tool handler | mitigate | Parse all input through Zod before response creation and return `INVALID_REQUEST`/`LIMIT_EXCEEDED` JSON for rejected requests. |
| T-01-11 | Repudiation | Successful response metadata | mitigate | Include `requestId`, server name/version, and ISO `generatedAt` in schema-valid responses for later audit linkage. |
</threat_model>

<verification>
Run:
```bash
npm install
npm test
npm run build
```
Then inspect docs with:
```bash
grep -v '^#' docs/mcp-contract.md | grep -q 'review_evidence'
grep -v '^#' docs/mcp-contract.md | grep -q 'INVALID_REQUEST'
grep -v '^#' docs/mcp-contract.md | grep -q 'no local file reads'
```
</verification>

<success_criteria>
Plan 02 is complete when valid requests produce schema-valid deterministic responses, invalid requests produce machine-readable sanitized errors, contract tests cover the Phase 1 MCP surface, and documentation tells a client how to run and invoke the server without promising later-phase provider or filesystem behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mcp-contract-and-skeleton/01-mcp-contract-and-skeleton-02-SUMMARY.md`.
</output>
